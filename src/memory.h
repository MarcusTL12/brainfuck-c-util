#ifndef __memory__
#define __memory__

void memset(int[] s, int c, int n) {
    for(int i = 0; , i < n, i = i + 1;) {
        s[i] = c;
    }
}

void memcpy(int[] dest, int[] src, int n) {
    for(int i = 0; , i < n, i = i + 1;) {
        dest[i] = src[i];
    }
}

#endif
